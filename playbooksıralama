#00_user_change_password     root ile çalıştır. root parolasını değiştirme
#01_user_managment           root ile çalıştır. kullanıcıları oluşturma(user-group-home-keys-rooterişimikapatma-parolasız root olmak)
#02_quick_env_info           root ile çalıştır ls22sistembackupfoleroluşturma-etc altındaki dosyaların yedeğini alma- key yedek alma- sudoers dosya yedeğinialma- PAKETLERİN YEDEĞİNİ "AL"
#03                          crowstrike_falcon velociraptor McAfee yükleme "CONF DOSYASININ YEDEĞİ ALINABİLİR Mİ"
#package_managment           paketleri güncelle kernel güncelle paket kaldır. 
#03_file_managment           dizin ve dosyaların permissions and owner group sıkılaştırmaları. linklenmiş dosyaların sıkılaştırmaları. paswordless user " yedekleri al"

ipv6 erişimlerini gt ye aç. kontrolünü sağla

ssh_hardening                ssh connections, root connection, password authentication no, max connect. , 
alias_hardening	              erkan bey
sudo_hardening               ser permissions - passwordless change root - include dir disable
userfile_hardening           home directroy - sudo group- shadow - having group number 0
terminal_hardening           history, ss, nyancat, unicorn vb terminal manipulation
cronjob_contol.yml
audit_hardening.yml         adnan bey tamamlandı
sourcelist_control.yml      adnan bey 
suid_guid_hardening         backups dosyasını oluştur. suid ve guid olanalrı buraya yedekle. nogroup olanalrı yedekle. tehlikeli dosyalarda suid guid olabilecek dosyalardan çıkar.

log4j emirbey adnan bey
selinux şimdilik askıda
ipv6 kontrolü

